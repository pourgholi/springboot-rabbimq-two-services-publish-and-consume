spring:
  datasource:
    username: engine-identity-db
    password: engine-identity-db
  jpa:
    hibernate:
      ddl-auto: create
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            scripts:
#              action: create
#              create-target: create.sql
#              create-source: metadata
